#include<stdio.h>

#define MAX 5

int main(){
        int contador = 0, i=0;
	/*
        estructura while
        while (condicion){ bloue de codigo } 
        */
        while(contador<MAX){
                contador = contador + 1
		contador++;
		printf("imprimiendo %d, \n", contador);
	}

	// bloue de codigo

        return 0;
}
